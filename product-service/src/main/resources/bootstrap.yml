spring:
  application:
    name: product-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
  data:
    mongodb:
        port: 27017
        host: user-mongodb
        database: microservicedatabase

  rabbitmq:
          host: rabbitmq
          port: 5672
          username: username
          password: password
server:
  port: 10001

